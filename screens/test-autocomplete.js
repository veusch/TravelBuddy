import { useState } from "react";
import { Text, StyleSheet, TouchableWithoutFeedback, Keyboard, TextInput, View } from "react-native";

export default function testAutocomplete() {
  // const[input,setInput] = useState<string>();
  //const onChangeText= async (text: string) => {
  //  setInput(text);
  // console.log("get Data");};
  //  return (
  // <TouchableWithoutFeedback onPress={()=>Keyboard.dismiss()}>
  //<View style={styles.container}>
  //  <Text>Search</Text>
  //<TextInput placeholder="find loc" value={input} onChangeText={onChangeText} style={{height:40,marginHorizontal:12,}}></TextInput>
  //</View>
  // </TouchableWithoutFeedback>
  //);
}

const styles = StyleSheet.create({
  container: { backgroundColor: "white", flex: 1 },
});
